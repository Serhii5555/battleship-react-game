# Storybook: Компоненти

Цей проект включає Storybook для інтерактивної розробки та демонстрації компонентів.

## 📖 Запуск Storybook

```bash
npm run storybook
```

Storybook буде доступний за адресою: **http://localhost:6006**

## 🎯 Реалізовані компоненти

### 1. **GameButton** (Базовий компонент)

Простий, універсальний кнопковий компонент для ігрових дій.

#### Властивості (Props):

- `children` (ReactNode) - текст кнопки
- `onClick` (function) - callback при кліку
- `disabled` (boolean) - вимкнення кнопки
- `type` ("button" | "submit" | "reset") - HTML тип кнопки
- `ariaLabel` (string) - label для screen readers (доступність)
- `className` (string) - додаткові CSS класи
- `overrideStyle` (CSSProperties) - inline стилі

#### 📚 Історії (Stories):

1. **Default** - стандартна кнопка "Play Game"
   - Демонструє базовий вигляд та функціональність
   - Включає onClick callback

2. **Primary** - основна дія з клас "primary"
   - Використовується для головних дій (Start Game)
   - Підходить для CTA (Call To Action)

3. **Disabled** - вимкнена кнопка
   - Показує стан при неактивній кнопці
   - Побудова завантажувача "Loading..."

4. **Large** - збільшена кнопка
   - Демонструє кастомне стилювання
   - Пропорційно збільшений padding та font-size

5. **SubmitButton** - submit тип кнопки
   - Для форм та фінальних дій
   - З aria-label для доступності

6. **WithAccessibility** - доступність
   - Screen reader label "Navigate to home page"
   - Демонструє важливість aria-label

---

### 2. **GameResultModal** (Комплексний компонент)

Складна модальна компонента для відображення результатів гри з анімаціями, динамічною стилізацією та управлінням знаходженнями.

#### Властивості (Props):

- `result` (GameResult | null) - об'єкт результатів гри з даними про пострілами, помадками, час
- `isOpen` (boolean) - контролює видимість модалі
- `onClose` (function) - callback при закритті модалі
- `onRetry` (function) - callback для повторної гри
- `onMainMenu` (function) - callback для повернення в меню

#### 🎬 Особливості:

- **Анімовані переходи** - modal scale/fade animations через Framer Motion
- **Динамічний фон** - зелений для перемоги, червоний для поразки
- **LocalStorage інтеграція** - збереження статистики ігрових сесій
- **Багатокнопкові дії** - Retry та Main Menu з различными callbacks
- **Форматування часу** - MM:SS формат для часу гри

#### 📚 Історії (Stories):

1. **Victory** - стандартна перемога
   - 5 кораблів знайдено з 12 пострілів
   - Green overlay для візуального визнання успіху
   - Час: 2 хвилини 5 секунд

2. **FastVictory** - швидка перемога
   - Мінімальна кількість пострілів (8)
   - Ідеально для демонстрації відмінної гри
   - Час: 45 секунд ⚡

3. **Defeat** - поразка гри
   - Red overlay для візуального позначення поразки
   - Знайдено тільки 3 з 5 кораблів
   - Час витрачено: 4 хвилини (240 сек)

4. **NarrowDefeat** - близька поразка
   - Гравець знайшов 4 з 5 кораблів (майже поруч!)
   - Демонструє "so close" сценарій
   - Час: 3 хвилини

5. **VictoryNoTimer** - перемога без таймера
   - Демонструє edge case коли timeElapsed відсутній
   - Показує "N/A" замість часу

6. **Closed** - прихована модаль
   - isOpen = false - модаль не видима
   - Демонструє поведінку коли модаль закрита

7. **Interactive** - повна інтерактивна демонстрація
   - State management для показу/приховання модалі
   - Кліки на "Retry" та "Main Menu" закривають модаль
   - "Show Modal" кнопка для повторного відкриття
   - Ідеально для тестування повного workflow

8. **ExtendedGameVictory** - довга гра
   - Демонструє результати після тривалої гри
   - 45 пострілів на 5 кораблів
   - Час: 8+ хвилин

---

## 🎮 Функціональність Controls (Керування)

У Storybook для кожного компонента доступні **Controls** панель, яка дозволяє:

- Змінювати props в реальному часі
- Бачити результат змін відразу
- Тестувати різні комбінації параметрів
- Копіювати згенерований код

### Приклади використання Controls:

**GameButton:**

- Змініть текст у полі "children"
- Залучіть/вимкніть "disabled" toggle
- Виберіть "type" з dropdown
- Спостерігайте за змінами в реальному часі

**GameResultModal:**

- Змініть `isOpen` для показу/приховання модалі
- Модифікуйте `result` об'єкт для різних стану (win/loss)
- Спостерігайте за динамічним фон-кольором
- Перевірьте callbacks в "Actions" вкладці

---

## 📱 Адаптивність

Компоненти повністю адаптивні:

- **GameButton** - сміливо розтягується на будь-яку ширину
- **GameResultModal** - fullscreen modal з centered контентом на мобільних

---

## 🧪 Тестування компонентів

Storybook дозволяє:

1. **Візуальне тестування** - переглядати різні стани
2. **Взаємодійне тестування** - клікати, конфігурувати, спостерігати callback'ї
3. **Доступність (a11y)** - перевіряти WCAG відповідність
4. **Документація** - читати опис та esemply props

### Вкладки в Storybook:

- **Canvas** - робочу область з компонентом
- **Docs** - автоматично згенерована документація
- **Controls** - інтерактивна панель конфігурації
- **Actions** - логування клавіш/callbacks
- **Accessibility** - перевірка доступності

---

## 🛠️ Структура проекту

```
src/
├── components/
│   ├── GameButton.tsx
│   ├── GameButton.stories.tsx           ✨ NEW
│   ├── GameResultModal.tsx
│   ├── GameResultModal.stories.tsx      ✨ NEW (Комплексний)
│   ├── SelectDropdown.tsx
│   └── ... інші компоненти
└── ... інші папки
```

---

## 📦 Додані залежності

При встановленні Storybook були додані:

- `storybook` - основний фреймворк
- `@storybook/react-vite` - інтеграція з React та Vite
- `@storybook/addon-docs` - документація
- `@storybook/addon-a11y` - доступність
- `@storybook/addon-vitest` - тестування
- `@chromatic-com/storybook` - Chromatic integration

---

## 🚀 Виконання завдання

✅ **Storybook встановлений та налаштований**
✅ **2 компоненти описані** (GameButton + GameResultModal)
✅ **Властивості конфігуруються** (через Controls)
✅ **Кожен компонент має 2-3+ варіацій:**

- GameButton (базовий): 6 stories
  - Default, Primary, Disabled, Large, SubmitButton, WithAccessibility

- GameResultModal (комплексний): 8 stories
  - Victory, FastVictory, Defeat, NarrowDefeat, VictoryNoTimer, Closed, Interactive, ExtendedGameVictory

✅ **Автоматична документація** (Docs tab)
✅ **Інтерактивне тестування** (Actions, Controls)
✅ **Складна компонента з анімаціями, state management та динамічною стилізацією**

---

## 📚 Корисні посилання

- [Storybook документація](https://storybook.js.org/)
- [Storybook Controls](https://storybook.js.org/docs/react/essentials/controls)
- [Storybook Actions](https://storybook.js.org/docs/react/essentials/actions)
- [React компоненти в Storybook](https://storybook.js.org/docs/react/get-started/introduction)

---

## 💡 Поради

- Використовуйте **Controls** для тестування різних пропс
- Перевіряйте **Actions** вкладку для відстеження callbacks
- Читайте **Docs** для детальної документації
- Тестуйте **Accessibility** для WCAG відповідності

Happy Storybook! 📖✨
